[package]
name = "nav-kv-package"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
lzma-rs = "0.3"
serde_json = "1"
zip = { version = "2.2", default-features = false }
