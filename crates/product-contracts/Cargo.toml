[package]
name = "product-contracts"
edition = "2021"
license = "AGPL-3.0-or-later"
version = "0.1.0"

[lib]
path = "src/lib.rs"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
