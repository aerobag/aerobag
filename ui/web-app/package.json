{
  "name": "aerobag-web",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "generate:symbols": "node ${AEROBAG_REPO_ROOT:-../..}/tools/generate-nav-symbols.mjs",
    "prepare:dev-assets": "./scripts/stage-dev-assets.sh",
    "build:wasm": "./scripts/build-wasm.sh",
    "install:wasm-opt": "./scripts/install-binaryen-wasm-opt.sh",
    "smoke:wasm-startup": "npm run build:wasm && node ./scripts/wasm-startup-smoke.mjs",
    "build:wasm:release": "./scripts/build-wasm.sh release",
    "build:wasm:optimized": "./scripts/build-wasm.sh optimized",
    "inner:dev": "npm run generate:symbols && npm run prepare:dev-assets && npm run build:wasm && vite",
    "inner:dev:fast": "npm run generate:symbols && npm run build:wasm && vite",
    "inner:dev:optimized-wasm": "npm run generate:symbols && npm run prepare:dev-assets && npm run build:wasm:optimized && vite",
    "inner:build": "npm run generate:symbols && npm run prepare:dev-assets && npm run build:wasm && tsc --noEmit && vite build",
    "inner:build:release": "npm run generate:symbols && npm run prepare:dev-assets && npm run build:wasm:release && tsc --noEmit && vite build",
    "inner:test": "npm run generate:symbols && vitest run",
    "dev": "./scripts/run-target-workspace.sh inner:dev",
    "dev:optimized-wasm": "./scripts/run-target-workspace.sh inner:dev:optimized-wasm",
    "build": "./scripts/run-target-workspace.sh inner:build",
    "build:release": "./scripts/run-target-workspace.sh inner:build:release",
    "test": "./scripts/run-target-workspace.sh inner:test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.4.1",
    "typescript": "^5.9.3",
    "vite": "^6.4.2",
    "vitest": "^3.2.4"
  }
}
