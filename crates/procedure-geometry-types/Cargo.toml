[package]
name = "procedure-geometry-types"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
had-key = { path = "../had-key" }
serde = { version = "1", features = ["derive"] }
