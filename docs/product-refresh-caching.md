# Warm product refreshes

## Implemented: completed TPP subgraphs

Both `build-cycle` and `build-product` look up a completed TPP region before
loading its render plan or expanding per-plate and per-thumbnail tasks. A hit
exposes the normal package result directly. A miss uses the existing fine-grained
graph; it does not delete or coarsen those caches.

The parent key includes source URLs and fetched content identity, region, version,
product contract, planner inputs, descendant source/scripts, and runtime tool
versions. Python bytecode generated by imports is excluded. The descendant node
versioning rules still apply: when changing a leaf's output semantics, update its
own key/version too. The conservative parent recipe forces re-expansion on code
changes; it does not invalidate unchanged leaves wholesale.

The completed parent stores one compact snapshot containing the package and all
child records. Warm builds emit these records into the normal build manifest and
GC root set. Thus existing GC still protects the fine-grained caches and referenced
fetch blobs, including across older and newer producer releases. Missing exposed
package outputs cause re-expansion. No new GC schema or legacy-manifest migration
is needed.

This removes per-child cache lookup, thread scheduling and task logging on hits.
It does **not** make all metadata processing constant-time: reading the snapshot,
emitting build manifests, and recording GC roots still scale with retained child
records. Nor does it skip fetching/checking current upstream source identities.
Look for `subgraph_cache_hit=true children_not_expanded` in the plan-task log.

## Implemented: chart visual-check nodes

`charts-<family>-quality` is a normal cached node. Its key covers fetched content
identity, observed source-file identity, cycle/family, chart metadata and approved
references, checker/cutline code, Python, NumPy and GDAL versions. File identity
uses device/inode/size/mtime, as the existing artifact verifier does; hardlink
ctime changes do not invalidate unchanged pixels. This relies on the existing
immutable fetch-cache model, not protection against deliberate metadata forgery.

The cached output is the completed verdict plus review evidence. Reusing it does
not rerun raster comparisons, create a checking attempt, or invent a new verified
timestamp. Actual cache misses preserve the existing family-serialized checking /
completed lifecycle and overdue-attempt monitoring. Blocked executions publish
diagnostics but do not create successful cache entries. A completed verdict that
explicitly quarantines some sheets can be reused; it retains that quarantine.

Rendering depends on the quality fingerprint and publication decision. Build
manifests retain the quality node for GC. Monitoring receives an atomic, bounded
copy of review evidence, not symlinks outside the HTTP handler's permitted tree.

## Proposed: relocatable per-build preprocessor tools

Not implemented in this change. The multi-version coordinator still builds in
ephemeral worktrees. Merely preserving its executable is unsafe: runtime code
uses the baked-in `CARGO_MANIFEST_DIR` to hash sources and locate Python scripts
and chart metadata after that directory has been removed.

Recommended boundary:

1. Compute source fingerprints at compile time, with repo-relative identities
   and explicit Cargo rebuild dependencies. Runtime code must not reopen Rust
   source files to identify the code it is executing.
2. Package runtime scripts and authored chart metadata in an immutable, explicit
   resource bundle alongside the binary. Resolve this bundle by a declared tool
   root, not the working directory or a search for an arbitrary live checkout.
3. Cache the binary and matching resource manifest as one validated build
   identity: exact source commit, toolchain/target/profile, relevant build flags
   and resource digests. Reuse an intact entry without invoking Cargo; retain the
   shared dependency target directory for real misses. Reject partial entries.
4. Retain tool entries for active release intents and in-progress builds; remove
   unreferenced entries through bounded ownership-aware GC. Do not create another
   indefinitely retained per-release Cargo target directory.
5. Test relocation by building the bundle, making the original checkout
   inaccessible, and running fingerprint/script/metadata-dependent commands from
   an unrelated directory. Include corruption, build-identity changes, concurrent
   creation and retirement tests.

Already-published producer revisions cannot gain this architecture retroactively.
They need the existing checkout-backed execution until retired, or an explicitly
retained, stable build-identity checkout. Do not silently run current producer
code on behalf of an old release.

## Validation and rollout

Regression coverage includes a generated one-plate TPP PDF through the real
render/thumbnail/package nodes, lazy warm expansion, parent invalidation with
leaf reuse, missing-output repair, and executed GC preserving child roots.
Chart fixtures exercise real GDAL comparisons, moved-sheet quarantine, explicit
reapproval, render/package propagation, repeated-hit reuse, interrupted attempts,
atomic evidence copying and bounded retention.

These Rust changes take effect in a newly built producer release; reconciling a
controller does not retrofit old producer binaries. The first run populates the
new parent and quality nodes (and quality-keyed render nodes); subsequent refreshes
get the savings. Production wall time has not yet been measured. Compare warm
refresh logs for the same release and unchanged inputs before setting a new
latency expectation.
